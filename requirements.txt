# Mini Viewer full-stack requirements.
# CUDA 12.4 PyTorch + gsplat are installed here; CPU mode still runs from this env
# with: --device cpu --backend torch
--index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://pypi.org/simple
--extra-index-url https://docs.gsplat.studio/whl/pt24cu124

# PyTorch / CUDA rendering stack.
torch==2.4.1
torchvision==0.19.1
gsplat==1.5.3+pt24cu124

# Core viewer / data / rendering dependencies.
numpy>=1.26,<2.3
plyfile>=1.0
pillow>=10.0
imageio[ffmpeg]>=2.34
matplotlib>=3.8
scikit-learn>=1.4
opencv-python>=4.9
viser>=1.0.15,<2
nerfview>=0.1.3
packaging>=24

# Language feature querying. The code loads these lazily.
open-clip-torch>=2.24
transformers>=4.49,<5
sentencepiece>=0.2
protobuf>=4.25
safetensors>=0.4
huggingface-hub>=0.24
hf-xet>=1.0
